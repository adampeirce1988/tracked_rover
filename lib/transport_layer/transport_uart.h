#ifndef UART_H
#define UART_H

#include <Arduino.h>
#include "transport.h" 
#include "debug.h"
#include "debug_config.h"

// // uart_io function ptototypes
// void uart_write(uint8_t byte);
// uint8_t uart_read();
// uint8_t uart_available();
// void uart_begin(uint32_t baud_rate);

//extern Transport_IO uart_io; 

#endif